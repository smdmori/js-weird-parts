// strict mode
function useStrict() {
    "use strict";
    // TODO: using strict mode locally in this function
    // If I add this string to the top of the file, it will behave globally

    var person2;
    persom2 = {}
    console.log(persom2);
}


// not using strict mode
var person;

persom = {};
console.log(persom);

useStrict();
